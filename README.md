# RotateComputerScreen

# This script rotates the computer’s primary screen in 90-degree increments. It uses the rotatescreen library and rotates the screen several times with a short pause between each rotation.
